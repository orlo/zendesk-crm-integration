{
    "name": "socialsignin/zendesk-crm-integration",
    "type": "project",
    "license": "BSD-3-Clause",
    "description": "Example code showing an Orlo CRM integraton with zendesk",
    "require": {
        "php" : "^8.0",
        "slim/slim": "^3.5",
        "zendesk/zendesk_api_client_php": "^2.1",
        "twig/twig": "^1.24",
        "monolog/monolog": "^1.23",
        "symfony/dotenv": "^5.2"
    },
    "authors": [
        {
            "name": "Orlo",
            "email": "technical+zendesk-crm@orlo.tech"
        }
    ],
    "require-dev": {
        "vimeo/psalm": "*"
    },
    "scripts" : {
        "psalm" : "@php -d grpc.enable_fork_support=1 -d grpc.poll_strategy=epoll1 ./vendor/bin/psalm --threads==$(nproc --all) --show-info=false --no-cache "
    }
}
